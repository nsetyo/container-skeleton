if [ -f "/etc/bash_completion" ] ; then
  . "/etc/bash_completion"
fi

for file in ~/source.d/*.sh ; do
  if [ -f "$file" ] ; then
    . "$file"
  fi
done

export PATH="~/.composer/vendor/bin:/var/www/backend/vendor/bin:$PATH"